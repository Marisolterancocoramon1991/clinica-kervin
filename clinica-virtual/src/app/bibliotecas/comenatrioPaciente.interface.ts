export interface ComentarioPaciente {
    idComentario: string;  // Identificador único para el comentario
    idTurno: string;       // ID del turno al que se refiere el comentario
    comentario: string;   // Comentario del paciente
}
