import { DownloadTablePdfDirective } from './download-table-pdf.directive';

describe('DownloadTablePdfDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadTablePdfDirective();
    expect(directive).toBeTruthy();
  });
});
