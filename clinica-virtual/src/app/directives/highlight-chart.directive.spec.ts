import { HighlightChartDirective } from './highlight-chart.directive';

describe('HighlightChartDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightChartDirective();
    expect(directive).toBeTruthy();
  });
});
